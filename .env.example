# Backend API URL (must end with /api)
# Local: http://localhost:5000/api
# Vercel Production: https://<your-backend-domain>/api
NEXT_PUBLIC_API_URL=https://fintrack-smart-finance-server.vercel.app/api

# Optional runtime mode marker
NEXT_PUBLIC_APP_ENV=production
